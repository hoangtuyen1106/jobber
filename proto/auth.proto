syntax = "proto3";

package auth;

service AuthService {
    rpc Authenticate(AuthenticateRequest) return (User)
}

message AuthenticateRequest {
    string token = 1;
}

message User {
    init32 id = 1;
    string email = 2;
}